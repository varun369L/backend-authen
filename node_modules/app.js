const express =require('express');
const dotenv = require("dotenv");
const cors = require("cors");


const app = express();
dotenv.config();


app.use("/",(req,res) =>{
    res.send('api is running')
 console.log("new router",req.url,req.method)

});

const PORT = 3001
app.listen(PORT, ()=>
console.log('server running on ${PORT}'));
